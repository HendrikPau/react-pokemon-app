import styled from 'styled-components'

export const GridContent = styled.div`
`

export const PokeList = styled.ul`
    height: 400px;
    overflow-y: scroll;
    list-style-type: none;
    padding-left: 0px;
`

export const PokeName = styled.li`
    background: #3a5d9f;
    padding: .5rem 1rem;
    margin: .4rem 0;
    border-radius: .3rem;
    color: #fdcc07;
`

export const StyledLabel = styled.label`
    font-size: 1.6rem;
    font-weight: bold;
    margin: 1rem 0;
`